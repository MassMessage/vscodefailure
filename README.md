Repo to reproduce the  Module not found: Can't resolve 'vscode' error

to build this run the commands:

```
yarn
yarn make
```

`yarn` install the needed packages and `yarn make` build the webpack.

then to run it:

```
yarn run dev
```